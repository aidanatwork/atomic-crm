# WARNING: Do NOT share your .env file (or the sensitive field values in it from SupaBase) 
# in version control or in production. 
# Add all .env files other than this example file to .gitignore.


SKIP_PREFLIGHT_CHECK=true

VITE_SUPABASE_URL=https://<project-id>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-public-api-key>
VITE_IS_DEMO=false
VITE_INBOUND_EMAIL=demo@example.com

# This is a fake JWT-style placeholder and NOT a real service role key.
# In production, get the real value from:
# Supabase UI → Project Settings → API Keys → Service Role
# SUPABASE_SERVICE_ROLE_KEY=abc.def.ghi-this-is-clearly-a-fake-key

# Dummy DATABASE_URL — looks real but is safe
# In production, find this under: Project Settings → Database → Connection Info
# DATABASE_URL=postgresql://postgres:password@db.project-id.supabase.co:5432/postgres